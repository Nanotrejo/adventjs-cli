interface YearAnswer {
  year: string;
}

interface TestsAnswer {
  tests: boolean;
}

interface VscodeAnswer {
  vscode: boolean;
}

interface DependenciesAnswer {
  dependencies: boolean;
}

export { YearAnswer, TestsAnswer, VscodeAnswer, DependenciesAnswer };
